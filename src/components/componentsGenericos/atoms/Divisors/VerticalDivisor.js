import styled from 'styled-components/native';

const VerticalDivisor = styled.View`
  background-color: ${({theme}) => theme.grayColor};
  height: 100%;
  width: 2px;
`;

export default VerticalDivisor;
