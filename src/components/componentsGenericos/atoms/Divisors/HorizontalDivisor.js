import styled from 'styled-components/native';

const HorizontalDivisor = styled.View`
  background-color: ${({theme}) => theme.grayColor};
  height: 2px;
  width: 100%;
`;

export default HorizontalDivisor;
