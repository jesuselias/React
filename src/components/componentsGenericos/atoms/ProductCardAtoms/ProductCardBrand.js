import styled from 'styled-components/native';
import {ExtraExtraSmallText} from '../Texts/SizedText';

const ProductCardBrand = styled(ExtraExtraSmallText)`
  text-transform: uppercase;
`;

export default ProductCardBrand;
