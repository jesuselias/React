import styled from 'styled-components/native';
import {SmallText} from '../Texts/SizedText';

const ProductCardShop = styled(SmallText)`
  font-family: ${({theme}) => theme.fontFamilyBold};
`;

export default ProductCardShop;
